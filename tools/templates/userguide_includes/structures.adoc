{% for structure in structures %}[[struct_{{ structure.anchor }}]]
=== {{ structure.name }}
{% if structure.used_in %}
Used in: {{ structure.used_in | join(', ') }}
{% endif %}
{% for property in structure.properties %}
[[{{ structure.anchor }}_{{ property.anchor }}]]
==== {{ property.name }}
[width=95%]
[cols="25h,75"]
|===
|Property name|*{{ property.name }}*
|Definition|{{ property.definition }}
{% if property.note %}|Notes|{{ property.note }}{% endif %}
|Type and cardinality|{{ property.type_cardinality }}
|Change history|{{ property.history if property.history else 'New in this version' }}
|XMP ID and data type|{{ property.xmp_id if property.xmp_id else 'N/A' }} ({{ property.xmp_type if property.xmp_type else 'N/A' }})
|JSON ID and data type|{{ property.json_schema_name if property.json_schema_name else 'N/A' }} ({{ property.json_schema_type if property.json_schema_type else 'N/A' }})
|===
{% endfor %}
{% endfor %}





